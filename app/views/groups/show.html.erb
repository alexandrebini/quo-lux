<% @group.products.each do |product| %>
  <% diff = product.diff(1.day.ago) %>
  <%= render partial: 'products/product_diff', locals: { product: product.diff(1.day.ago) } %>
<% end %>

<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>
